import { expect } from 'chai';

describe('Support', () => {
  it('should do a thing', () => {
    expect(true).to.equal(true);
  })

  it('should do a thing', () => {
    expect(true).to.equal(false);
  })
})
